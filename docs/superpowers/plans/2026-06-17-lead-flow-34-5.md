# Lead-Flow #3 #4 #5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add optional E-Mail-Feld (#4), move LeadForm in den Hero (#3), SimplyMeet.me Buchungs-CTA (#5).

**Architecture:** Alle Änderungen in zwei Dateien: `LeadForm.tsx` (Email-Feld + neues `bookingUrl`-Prop) und `[niche].astro` (Hero-Umbau + Booking-CTA). Kein neuer Branch nötig — kleine additive Änderungen direkt auf main.

**Tech Stack:** Astro 4, React (TSX), Tailwind CSS, TypeScript

---

### Task 1: E-Mail-Feld optional in LeadForm (#4)

**Files:**
- Modify: `src/components/LeadForm.tsx`

- [ ] **Step 1: email-State und Prop hinzufügen**

In `LeadForm.tsx`, Props-Interface erweitern und State ergänzen:

```tsx
// Props-Interface (nach `source?: string`):
bookingUrl?: string

// Nach `const [instagram, setInstagram] = useState('')`:
const [email, setEmail] = useState('')
```

- [ ] **Step 2: Email-Feld nach Telefon einfügen**

Nach dem `</div>` des Telefon-Feldes (Zeile ~268), vor dem Instagram-Feld einfügen:

```tsx
<div>
  <label htmlFor="email" className={labelClass}>E-Mail <span className="text-white/30 font-light normal-case tracking-normal">(optional)</span></label>
  <input
    id="email"
    name="email"
    type="email"
    placeholder="max@muster.de"
    value={email}
    onChange={e => setEmail(e.target.value)}
    autoComplete="email"
    inputMode="email"
    className={inputClass}
  />
</div>
```

- [ ] **Step 3: Email in handleSubmit-Payload aufnehmen**

Im `body: JSON.stringify({...})` Block, nach `telefon: data.telefon`:

```tsx
email: (data.email as string) ?? '',
```

- [ ] **Step 4: Formular-Hinweis anpassen**

Zeile ~237: Text von `'2 Pflichtfelder · 30 Sekunden'` zu:

```tsx
<p className="text-xs tracking-[0.2em] uppercase text-white/55 mb-1">2 Pflichtfelder · E-Mail optional · 30 Sekunden</p>
```

- [ ] **Step 5: Committen**

```bash
git add src/components/LeadForm.tsx
git commit -m "feat(form): add optional email field"
```

---

### Task 2: Booking-URL-Prop + CTA in LeadForm (#5 vorbereitung)

**Files:**
- Modify: `src/components/LeadForm.tsx`

- [ ] **Step 1: bookingUrl im idle-State rendern**

Am Ende des Formulars, nach dem Datenschutz-Hinweis (`<p className="text-xs tracking-widest ...`), vor dem schließenden `</form>`:

```tsx
{bookingUrl && (
  <p className="text-center text-xs text-white/40 pt-1">
    Lieber erst informieren?{' '}
    <a
      href={bookingUrl}
      target="_blank"
      rel="noopener noreferrer"
      onClick={() => analytics.demoBooking('form')}
      className="text-white/60 hover:text-gold-600 underline underline-offset-2 transition-colors"
    >
      Kostenlosen Termin buchen →
    </a>
  </p>
)}
```

- [ ] **Step 2: Committen**

```bash
git add src/components/LeadForm.tsx
git commit -m "feat(form): add optional booking URL link below form"
```

---

### Task 3: Hero-Umbau — Form rechts, kleines Mockup als Trust-Element (#3)

**Files:**
- Modify: `src/pages/koeln/[veedel]/[niche].astro`

- [ ] **Step 1: Hero rechte Spalte ersetzen**

Die gesamte rechte Spalte im Hero (Zeilen ~167–174):

```astro
<!-- ALT: -->
<div class="flex justify-center lg:justify-end">
  <WalletMockup ... />
</div>
```

Ersetzen mit:

```astro
<div class="flex flex-col gap-6 items-center md:items-end">
  {nicheData.formType === 'simple' ? (
    <WalletMockup
      businessName={nicheData.walletMockup.businessName}
      reward={nicheData.walletMockup.reward}
      stamped={nicheData.walletMockup.stamped}
      total={nicheData.walletMockup.total}
    />
  ) : (
    <>
      <div class="opacity-60 scale-75 origin-top hidden md:block">
        <WalletMockup
          businessName={nicheData.walletMockup.businessName}
          reward={nicheData.walletMockup.reward}
          stamped={nicheData.walletMockup.stamped}
          total={nicheData.walletMockup.total}
        />
      </div>
      <div id="demo" class="w-full border border-gold-600/20 bg-black/60 backdrop-blur-sm p-6 sm:p-8">
        <LeadForm
          client:load
          niche={nicheData.label}
          city="Köln"
          whatsappUrl={whatsappUrl}
          bookingUrl="https://termin.fuerte.digital"
        />
      </div>
    </>
  )}
</div>
```

- [ ] **Step 2: Bottom-Demo-Sektion anpassen**

Die `<section id="demo" ...>` (Zeile ~292) für Standard-Seiten: `id="demo"` entfernen (Anker ist jetzt im Hero) und das Formular durch einen einfachen Scroll-CTA ersetzen. Für Döner bleibt alles unverändert.

Vollständigen Ersatz des `<!-- ── LEAD FORM ──>` Blocks:

```astro
<!-- ── LEAD FORM ─────────────────────────────────────────────── -->
{nicheData.formType === 'simple' ? (
  <section id="demo" class="py-12 sm:py-16 px-5 sm:px-8 bg-black relative overflow-hidden">
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[400px] bg-gold-600 rounded-full blur-[180px] opacity-5"></div>
    <div class="relative max-w-xl mx-auto">
      <div class="text-center mb-10">
        <div class="inline-flex items-center gap-3 border border-gold-600/30 bg-gold-600/5 px-4 py-2 mb-8">
          <Icon name="diamond" class="w-4 h-4 text-gold-600 shrink-0" />
          <span class="text-gold-500 text-xs tracking-[0.3em] uppercase">
            Exklusiv für {nicheData.emoji} in {veedelData.name}
          </span>
        </div>
        <h2 class="text-3xl sm:text-4xl font-extralight text-white tracking-tight mb-2">Demo-Karte</h2>
        <h2 class="text-3xl sm:text-4xl font-bold text-white tracking-tight mb-6">direkt ins Wallet.</h2>
        <p class="text-white text-base font-light leading-relaxed max-w-sm mx-auto">
          Trag dich ein – deine persönliche Demo-Karte liegt in Sekunden in deinem Wallet.
        </p>
      </div>
      <div class="relative border border-gold-600/20 bg-ink-950 p-8 sm:p-10">
        <LeadFormDoener client:load whatsappUrl={whatsappUrl} niche={nicheData.slug} />
      </div>
      <p class="text-center text-white text-sm mt-5">
        Nach 90 Tagen ab 29 €/Monat ·
        <a href="/preise" class="text-gold-600 hover:text-gold-600 underline underline-offset-2 transition-colors">Alle Preise ansehen</a>
      </p>
    </div>
  </section>
) : (
  <section class="py-10 sm:py-12 px-5 sm:px-8 bg-black border-t border-white/5 md:hidden">
    <div class="max-w-xl mx-auto text-center">
      <p class="text-white/50 text-sm mb-4">Noch nicht überzeugt?</p>
      <a href="#demo"
        class="group inline-flex items-center gap-3 border border-gold-600/40 hover:border-gold-600 text-gold-600 px-8 py-4 text-sm font-medium tracking-[0.15em] uppercase transition-all">
        {nicheData.heroCopy.ctaLabel}
        <svg class="w-3.5 h-3.5 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
        </svg>
      </a>
    </div>
  </section>
)}
```

- [ ] **Step 3: Lokalen Build testen**

```bash
npm run build && echo "BUILD OK"
```
Erwartung: keine Build-Fehler, kein TypeScript-Fehler.

- [ ] **Step 4: Committen**

```bash
git add src/pages/koeln/[veedel]/[niche].astro
git commit -m "feat(hero): move LeadForm into hero right column, WalletMockup as trust element"
```

---

### Task 4: Booking-CTA im Hero (#5)

**Files:**
- Modify: `src/pages/koeln/[veedel]/[niche].astro`

- [ ] **Step 1: Dritten CTA unter den WhatsApp-Button einfügen**

Im Hero, nach dem schließenden `</a>` des WhatsApp-Buttons (Zeile ~148), vor dem schließenden `</div>` der Button-Gruppe:

```astro
<a href="https://termin.fuerte.digital"
  target="_blank"
  rel="noopener noreferrer"
  onclick="if(typeof gtag!=='undefined')gtag('event','demo_booking',{source:'hero'})"
  class="inline-flex items-center gap-2 text-white/50 hover:text-white/80 text-xs tracking-[0.15em] uppercase transition-colors py-1">
  <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
  </svg>
  Erst informieren? Termin buchen
</a>
```

- [ ] **Step 2: Committen**

```bash
git add src/pages/koeln/[veedel]/[niche].astro
git commit -m "feat(hero): add booking CTA link to termin.fuerte.digital"
```

---

### Task 5: Push + Browser-Test

- [ ] **Step 1: Pushen**

```bash
git push origin main
```

- [ ] **Step 2: Deploy abwarten**

```bash
gh run list --limit 1
# Warten bis "completed success"
```

- [ ] **Step 3: Desktop-Test**

Browser öffnen: https://bonuskarte.digital/koeln/nippes/cafes

Prüfen:
- Hero rechts: WalletMockup (klein, gedimmt) + Formular
- Formular hat: Vorname*, Handynummer*, E-Mail (optional), Instagram
- Unter dem Formular: „Lieber erst informieren? Kostenlosen Termin buchen →"
- Unter den Hero-CTAs: „Erst informieren? Termin buchen" Link

- [ ] **Step 4: Mobile-Test**

DevTools → Mobile (375px Breite):
- Copy + kleines Mockup unsichtbar (hidden md:block)
- Form ist direkt sichtbar unter dem Copy
- Alle Felder korrekt

- [ ] **Step 5: Döner-Seite prüfen (Regression)**

https://bonuskarte.digital/koeln/nippes/doener prüfen:
- Hero rechts: WalletMockup (unverändert)
- Bottom-Sektion: LeadFormDoener wie bisher

- [ ] **Step 6: Buchungs-Link testen**

Klick auf „Erst informieren?" → https://termin.fuerte.digital öffnet in neuem Tab
